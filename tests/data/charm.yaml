description: Overlay to attach a local Kubernetes Worker charm.
applications:
  kubernetes-worker:
    charm: {{charm}}
    channel: null
    resources:
      cni-plugins: {{resource_path}}/cni-plugins-{{arch}}.tar.gz
